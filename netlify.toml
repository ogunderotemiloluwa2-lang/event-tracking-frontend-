# Netlify build configuration — builds from source (aligned with Vercel)
[build]
  command = "CI=false npm install && npm run build"
  publish = "build"

# SPA fallback: serve index.html for any path (also covered by public/_redirects).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
